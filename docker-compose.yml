services:
  backend:
    build: ./backend
    ports:
      - "2343:2343"
    environment:
      - PORT=2343
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
